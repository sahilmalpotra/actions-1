name: First-workflow

on: push

jobs: 
    first_job:
        runs-on: ubuntu-latest
        steps:
            - name: check repo
              uses: actions/checkout@v4

            - name: welcome message
              run: echo "first action"
            
            - name: list files
              run: ls
              
            - name: read file
              run: cat README.md  